{
  "name": "propertyscope",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "boundary-check": "node scripts/assert-isolated-repo.mjs",
    "verify": "npm run boundary-check && npm run protected-adapter-check && npm run correlation-check && npm run typecheck && npm run build",
    "protected-adapter-check": "node scripts/protected-adapter-check.mjs",
    "correlation-check": "node --experimental-strip-types --test tests/propertyscope-safe-correlation.test.mjs tests/propertyscope-vercel-oidc-transport.test.mjs tests/propertyscope-hdp-rejection-observability.test.mjs"
  },
  "dependencies": {
    "next": "^16.2.10",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=20.9.0"
  },
  "overrides": {
    "postcss": "8.5.20"
  }
}
